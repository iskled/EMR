import { Link } from 'react-router-dom'
export default function TodaySchedule({ data }) { return <section className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex justify-between"><h2 className="text-lg font-bold">Today’s schedule</h2><Link to="/appointments" className="text-sm font-semibold text-blue-700">Open schedule</Link></div>{data.items.length === 0 ? <p className="py-10 text-center text-slate-500">No appointments scheduled today.</p> : <div className="mt-4 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="text-xs uppercase text-slate-500"><tr><th className="py-2">Time</th><th>Patient</th><th>Type</th><th>Dentist</th><th>Duration</th><th>Status</th><th></th></tr></thead><tbody>{data.items.map(a => <tr key={a.id} className="border-t"><td className="py-3 font-semibold">{a.time}</td><td>{a.patient}{a.is_orthodontic && <span title="Orthodontic" className="ml-2 rounded bg-violet-100 px-2 py-0.5 text-xs text-violet-700">Ortho</span>}</td><td>{a.appointment_type}</td><td>{a.dentist}</td><td>{a.duration_minutes} min</td><td className="capitalize">{a.status.replaceAll('_',' ')}</td><td><Link to={`/appointments?appointment=${a.id}`} className="text-blue-700">Open</Link></td></tr>)}</tbody></table></div>}</section> }
