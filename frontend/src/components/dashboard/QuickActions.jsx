export default function QuickActions({capabilities={},onAction}){const actions=[[capabilities['patients.write'],'patient','New Patient'],[capabilities['appointments.write'],'appointment','New Appointment'],[capabilities['inventory.usage'],'inventory','Record Inventory Usage'],[capabilities['tasks.write'],'task','Create Task']].filter(x=>x[0]);return <section><h2 className="mb-3 text-sm font-bold uppercase tracking-wide text-slate-600">Quick actions</h2><div className="flex flex-wrap gap-2">{actions.map(([,key,label])=><button type="button" key={key} onClick={()=>onAction(key)} className="rounded-lg border border-slate-300 bg-white px-5 py-3 text-base font-bold hover:border-blue-500">{label}</button>)}</div></section>}
