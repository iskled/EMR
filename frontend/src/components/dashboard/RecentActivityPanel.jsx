export default function RecentActivityPanel({ data }) { return <section className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Recent activity</h2>{data.length?<ul className="mt-3 divide-y">{data.map(x=><li key={x.id} className="py-2 text-sm"><span className="font-medium capitalize">{x.action.replaceAll('_',' ')}</span> · {x.resource_type}<time className="float-right text-xs text-slate-500">{new Date(x.timestamp).toLocaleString()}</time></li>)}</ul>:<p className="mt-4 text-sm text-slate-500">No recent activity available for your account.</p>}</section> }
