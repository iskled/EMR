import { Link } from 'react-router-dom'
const labels = {appointments_today:"Appointments today",scheduled:'Scheduled',confirmed:'Confirmed',in_progress:'In progress',completed:'Completed',cancelled:'Cancelled',no_show:'No-show',active_patients:'Active patients',new_patients_month:'New patients this month',active_orthodontic_cases:'Active ortho cases',orthodontic_reviews_due:'Ortho reviews due',open_tasks:'Open tasks',overdue_tasks:'Overdue tasks',urgent_tasks:'Urgent tasks',blocked_tasks:'Blocked tasks',low_stock:'Low stock',out_of_stock:'Out of stock',expiring_inventory:'Expiry actions',unsigned_notes:'Unsigned notes',recalls_due:'Recalls due'}
const pathFor = key => key.includes('task') ? '/tasks' : key.includes('stock') || key.includes('inventory') ? '/inventory' : key.includes('ortho') ? '/orthodontics' : key.includes('patient') || key.includes('recall') || key.includes('note') ? '/patients' : '/appointments'
export default function DashboardMetrics({ metrics }) { return <section aria-labelledby="metrics-title"><h2 id="metrics-title" className="sr-only">Key metrics</h2><div className="grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-6">{Object.entries(metrics).filter(([k]) => labels[k]).map(([key,value]) => <Link to={pathFor(key)} key={key} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition hover:border-blue-300 hover:shadow"><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{labels[key]}</p><p className="mt-2 text-3xl font-bold text-slate-900">{value}</p></Link>)}</div></section> }
