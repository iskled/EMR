export default function PatientFlowPanel({ appointments }) { const active=appointments.items.filter(x=>x.status==='in_progress'); return <section className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Patient flow</h2>{active.length ? <ul className="mt-3 divide-y">{active.map(x=><li key={x.id} className="py-3"><span className="font-semibold">{x.patient}</span><span className="float-right text-sm text-slate-500">In treatment · {x.dentist}</span></li>)}</ul> : <p className="mt-4 text-sm text-slate-500">No patients currently in treatment. A separate checked-in workflow is not available in the current appointment model.</p>}</section> }
