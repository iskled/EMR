import { Link } from 'react-router-dom'
export default function ClinicalAlertsPanel({ data }) { return <section className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Clinical actions</h2>{data.alerts.length ? <ul className="mt-3 divide-y">{data.alerts.map((x,i)=><li key={`${x.type}-${i}`} className="py-3"><Link to={`/patients?patient=${x.patient_id}`} className="font-semibold text-blue-800">{x.patient}</Link><p className="text-sm text-slate-500">{x.message}</p></li>)}</ul>:<p className="mt-4 text-sm text-slate-500">No clinical actions are due.</p>}</section> }
