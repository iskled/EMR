export default function Tabs({ tabs = [], activeTab, onChange }) { return (<div className='border-b border-gray-200 flex gap-4'>{tabs.map((tab) => (<button key={tab} onClick={() => onChange(tab)} className={`px-4 py-2 text-sm font-medium border-b-2 transition ${activeTab === tab ? 'border-blue-600 text-blue-600' : 'border-transparent text-gray-500 hover:text-gray-700'}`}>{tab}</button>))}</div>) }